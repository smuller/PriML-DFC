priority high
priority med
priority low

order low < med
order med < high

(* constraint violation {med} <= {high, low} *)
main {

    t <- spawn[if true then high else low] {
        ret 1
    };

    spawn[med] {
        x <- sync t;
        ret x
    }
}
